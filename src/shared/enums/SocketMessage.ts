export enum SocketMessage {
	CONNECTION = 'connection',
	VALIDATE = 'validate',
	JOINED = 'joined',
	VOTED = 'voted',
	USERS = 'users',
	CLEAR = 'clear',
	ERROR = 'error',
	DISCONNECT ='disconnect'
}
