export enum SocketMessage {
	CONNECTION = 'connection',
	VALIDATE = 'validate',
	ISVALID = 'isvalid',
	ROOM = 'room',
	JOINED = 'joined',
	VOTED = 'voted',
	USERS = 'users',
	CLEAR = 'clear',
	ERROR = 'error',
	DISCONNECT ='disconnect'
}
